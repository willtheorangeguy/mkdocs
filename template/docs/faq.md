<!--
  FAQ

  Include this page in EVERY repo. Do not wait for real questions to
  accumulate — the repos with none recorded are the ones where a reader has
  nowhere else to turn.

  Read "Writing an FAQ" in docs.instructions.md before filling this in.

  The rule in one line: anticipate the question, never invent the answer.
  Predicting what someone will ask is judgement, and you have just read the
  source, the tests, and the workflows — use it. Every ANSWER must trace to
  behaviour you actually verified.

  Best sources of questions, in order:
    1. Anything that surprised you while reading the source. It will surprise
       the reader too.
    2. Gaps between what the README promises and what the code does.
    3. The first five minutes: install fails, command not on PATH, program
       exits immediately, nothing appears.
    4. Decisions the reader must make: which install method, which image tag.
    5. Whatever a prompt, placeholder, or error message hints at.

  Four sharp entries beat twelve filler ones. Drop anything whose answer is
  just "read the installation page".
-->

# FAQ

## Common questions

<!-- Phrase each as the reader would type it into a search box. First entry
     uses ???+ so the page opens with something visible. -->

???+ question "TODO: the question a new user hits first"

    <!-- Answer from verified behaviour, with a command or link where one
         applies. -->

??? question "TODO: the behaviour that surprised you in the source"

    <!-- ... -->

??? question "TODO: a decision the reader has to make"

    <!-- ... -->

## Troubleshooting

<!--
  Failures with a visible symptom go here rather than above, keyed on the
  error text the reader will paste into a search engine.

  Structure each as: symptom, cause, fix. Lead with the symptom.
-->

### <!-- TODO: the error message or symptom, verbatim -->

**Cause.** <!-- TODO -->

**Fix.**

```bash
# TODO
```

## Getting help

{{ support() }}

When reporting a problem, include:

- The version you are running and how you installed it
- Your operating system
- The exact command you ran and its complete output
